# Build 8.0.8 - Complete Flow Diagram

## 🔄 User Journey Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER OPENS AURORALINK                         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              BROWSER LOADS APP.TSX FILE                          │
│                    (React not yet started)                       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│  LINE 2: import './utils/nuclear-session-cleaner';              │
│  ➤ This file executes IMMEDIATELY at import time                │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
                ┌────────────────────┐
                │ NUCLEAR CLEANER    │
                │ CHECKS VERSION     │
                └─────────┬──────────┘
                          │
            ┌─────────────┴─────────────┐
            │                           │
            ▼                           ▼
┌──────────────────────┐     ┌──────────────────────┐
│ Version Matches      │     │ Version Doesn't      │
│ v8.0.8-nuclear-      │     │ Match OR             │
│ 20251102             │     │ Doesn't Exist        │
└─────────┬────────────┘     └──────────┬───────────┘
          │                             │
          │                             ▼
          │                  ┌──────────────────────┐
          │                  │ 🚨 NUCLEAR CLEAR 🚨  │
          │                  │                      │
          │                  │ 1. localStorage      │
          │                  │    .clear()          │
          │                  │                      │
          │                  │ 2. sessionStorage    │
          │                  │    .clear()          │
          │                  │                      │
          │                  │ 3. Clear IndexedDB   │
          │                  │                      │
          │                  │ 4. Set flag:         │
          │                  │    v8.0.8-nuclear-   │
          │                  │    20251102          │
          │                  │                      │
          │                  │ ✅ COMPLETE          │
          │                  └──────────┬───────────┘
          │                             │
          │                             │
          └─────────────┬───────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│              OTHER IMPORTS RUN (App continues loading)           │
│  - aggressive-token-cleaner.ts                                  │
│  - immediate-session-validator.ts                               │
│  - other utilities                                              │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    REACT INITIALIZES                             │
│                    APP COMPONENT RENDERS                         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
                ┌────────────────────┐
                │ CHECK AUTH STATE   │
                │ Do we have valid   │
                │ session?           │
                └─────────┬──────────┘
                          │
            ┌─────────────┴─────────────┐
            │                           │
            ▼                           ▼
┌──────────────────────┐     ┌──────────────────────┐
│ YES - Have Session   │     │ NO - No Session      │
│                      │     │ (Most likely after   │
│ User stayed logged   │     │  nuclear clear)      │
│ in from before       │     │                      │
└─────────┬────────────┘     └──────────┬───────────┘
          │                             │
          ▼                             ▼
┌──────────────────────┐     ┌──────────────────────┐
│ RENDER HOME SCREEN   │     │ RENDER LOGIN SCREEN  │
│                      │     │                      │
│ - Conversations      │     │ ┌──────────────────┐ │
│ - Messages           │     │ │ Email:           │ │
│ - Stories            │     │ │ [_______________]│ │
│ - All features       │     │ │                  │ │
│                      │     │ │ Password:        │ │
│                      │     │ │ [_______________]│ │
│                      │     │ │                  │ │
│                      │     │ │  [  LOG IN  ]    │ │
│                      │     │ └──────────────────┘ │
└─────────┬────────────┘     └──────────┬───────────┘
          │                             │
          │                             ▼
          │                  ┌────────────────��─────┐
          │                  │ USER ENTERS          │
          │                  │ CREDENTIALS          │
          │                  └──────────┬───────────┘
          │                             │
          │                             ▼
          │                  ┌──────────────────────┐
          │                  │ CLICK "LOG IN"       │
          │                  └──────────┬───────────┘
          │                             │
          │                             ▼
          │                  ┌──────────────────────┐
          │                  │ API: POST /login     │
          │                  └──────────┬───────────┘
          │                             │
          │                   ┌─────────┴──────────┐
          │                   │                    │
          │                   ▼                    ▼
          │         ┌──────────────────┐  ┌────────────────┐
          │         │ SUCCESS          │  │ ERROR          │
          │         │ - Got JWT token  │  │ - Wrong creds  │
          │         │ - Save session   │  │ - Show error   │
          │         └─────────┬────────┘  └────────────────┘
          │                   │
          │                   ▼
          │         ┌──────────────────────┐
          │         │ REDIRECT TO HOME     │
          │         └──────────┬───────────┘
          │                    │
          └────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                    APP RUNNING NORMALLY                          │
│                                                                  │
│  User can:                                                       │
│  - Send/receive messages                                        │
│  - Make voice/video calls                                       │
│  - View/post stories                                            │
│  - Update profile                                               │
│  - All features work                                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                 USER MAKES API CALLS                             │
│              (e.g., send message, load conversations)            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
                ┌────────────────────┐
                │ API Client         │
                │ /utils/api.ts      │
                │                    │
                │ Gets token from    │
                │ session storage    │
                │                    │
                │ Adds to Auth       │
                │ header             │
                └─────────┬──────────┘
                          │
                          ▼
                ┌────────────────────┐
                │ SEND REQUEST       │
                │ to Server          │
                └─────────┬──────────┘
                          │
            ┌─────────────┴─────────────┐
            │                           │
            ▼                           ▼
┌──────────────────────┐     ┌──────────────────────┐
│ SUCCESS (200)        │     │ AUTH ERROR           │
│                      │     │ - 401 Unauthorized   │
│ Return data to       │     │ - "Auth session      │
│ component            │     │   missing!"          │
└──────────────────────┘     └──────────┬───────────┘
                                        │
                                        ▼
                             ┌──────────────────────┐
                             │ ENHANCED ERROR       │
                             │ HANDLER TRIGGERS     │
                             │                      │
                             │ Detects:             │
                             │ - Status 401         │
                             │ - OR "Auth session"  │
                             │   in message         │
                             └──────────┬───────────┘
                                        │
                                        ▼
                             ┌──────────────────────┐
                             │ IMMEDIATE ACTION:    │
                             │                      │
                             │ 1. localStorage      │
                             │    .clear()          │
                             │                      │
                             │ 2. sessionStorage    │
                             │    .clear()          │
                             │                      │
                             │ 3. Console log:      │
                             │    "SESSION INVALID  │
                             │     - LOGGING OUT"   │
                             │                      │
                             │ 4. Wait 1.5 seconds  │
                             │                      │
                             │ 5. Redirect:         │
                             │    window.location   │
                             │    .href = origin    │
                             └──────────┬───────────┘
                                        │
                                        ▼
                             ┌──────────────────────┐
                             │ PAGE RELOADS         │
                             └──────────┬───────────┘
                                        │
                                        ▼
                             ┌──────────────────────┐
                             │ BACK TO START        │
                             │ (Nuclear cleaner     │
                             │  already ran, so     │
                             │  it skips)           │
                             │                      │
                             │ User sees LOGIN      │
                             │ screen               │
                             └──────────────────────┘
```

---

## 🔄 Reload Behavior

### First Reload (After Nuclear Clear):

```
USER RELOADS PAGE
       │
       ▼
Nuclear Cleaner Runs
       │
       ▼
Checks: localStorage.getItem('aurora_nuclear_clear')
       │
       ▼
Returns: "v8.0.8-nuclear-20251102" (already set)
       │
       ▼
┌──────────────────┐
│ Version Matches  │
│ ➤ SKIP CLEAR    │
│ ➤ Continue      │
└────────┬─────────┘
         │
         ▼
App Continues Loading
         │
         ▼
Check Session
         │
    ┌────┴────┐
    │         │
    ▼         ▼
 Logged In   Not Logged In
    │            │
    ▼            ▼
  Home         Login
  Screen       Screen
```

---

## 📊 Version Check Logic

```
┌──────────────────────────────────────────┐
│         NUCLEAR VERSION CHECK            │
└──────────────────────────────────────────┘

const NUCLEAR_VERSION = 'v8.0.8-nuclear-20251102'
const stored = localStorage.getItem('aurora_nuclear_clear')

IF stored === null
   ➤ PERFORM NUCLEAR CLEAR
   ➤ Set flag to NUCLEAR_VERSION

ELSE IF stored !== NUCLEAR_VERSION
   ➤ PERFORM NUCLEAR CLEAR
   ➤ Update flag to NUCLEAR_VERSION

ELSE IF stored === NUCLEAR_VERSION
   ➤ SKIP CLEAR
   ➤ Log: "Already performed"

┌──────────────────────────────────────────┐
│         EXAMPLE SCENARIOS                │
└──────────────────────────────────────────┘

Scenario 1: Brand new user
stored = null
➤ CLEAR (set flag)

Scenario 2: First load of Build 8.0.8
stored = "v8.0.7-something" (from previous build)
➤ CLEAR (update flag to v8.0.8)

Scenario 3: Second load of Build 8.0.8
stored = "v8.0.8-nuclear-20251102"
➤ SKIP (already cleared)

Scenario 4: Future build 8.0.9
stored = "v8.0.8-nuclear-20251102"
NUCLEAR_VERSION = "v8.0.9-nuclear-20251103"
➤ CLEAR (update flag to v8.0.9)
```

---

## 🛡️ Error Recovery Flow

```
API CALL MADE
     │
     ▼
┌─────────────────────┐
│ Request sent with   │
│ Authorization:      │
│ Bearer <token>      │
└──────────┬──────────┘
           │
           ▼
    Server validates
           │
     ┌─────┴─────┐
     │           │
     ▼           ▼
┌─────────┐  ┌──────────────────┐
│ Valid   │  │ Invalid Token    │
│ Token   │  │ - Wrong project  │
│         │  │ - Expired        │
│         │  │ - Revoked        │
└────┬────┘  └────────┬─────────┘
     │                │
     ▼                ▼
┌─────────┐  ┌──────────────────┐
│ Return  │  │ Return 401 OR    │
│ Data    │  │ "Auth session    │
│ (200)   │  │  missing!"       │
└─────────┘  └────────┬─────────┘
                      │
                      ▼
           ┌──────────────────────┐
           │ /utils/api.ts        │
           │ Error Handler        │
           │                      │
           │ IF status === 401 OR │
           │ error.includes(      │
           │   'Auth session'     │
           │ )                    │
           └──────────┬───────────┘
                      │
                      ▼
           ┌──────────────────────┐
           │ IMMEDIATE LOGOUT     │
           │                      │
           │ 1. Clear storage     │
           │ 2. Log message       │
           │ 3. Redirect to login │
           └──────────┬───────────┘
                      │
                      ▼
           ┌──────────────────────┐
           │ User sees:           │
           │ LOGIN SCREEN         │
           │                      │
           │ Clear message:       │
           │ "Session expired,    │
           │  please log in"      │
           └──────────────────────┘
```

---

## 🔐 Token Lifecycle

```
┌─────────────────────────────────────────────┐
│           TOKEN LIFECYCLE                   │
└─────────────────────────────────────────────┘

USER LOGS IN
    │
    ▼
Server creates JWT
    │
    ├─ access_token (1 hour)
    ├─ refresh_token (30 days)
    ├─ exp: timestamp
    └─ iss: https://project.supabase.co/auth/v1
    │
    ▼
Token saved to:
localStorage['sb-{project}-auth-token']
    │
    ▼
Token used for API calls
    │
    ├─ Added to Authorization header
    ├─ Server validates each time
    └─ Returns data if valid
    │
    ▼
Token can become invalid:
    │
    ├─ ❌ Expired (past exp time)
    ├─ ❌ Wrong project (iss mismatch)
    ├─ ❌ Revoked server-side
    └─ ❌ Corrupted format
    │
    ▼
NUCLEAR CLEANER (v8.0.8)
OR Error Handler
catches invalid token
    │
    ├─ Clear localStorage
    ├─ Clear sessionStorage
    └─ Force re-login
    │
    ▼
BACK TO: USER LOGS IN
```

---

## 📱 Multi-Device Behavior

```
┌─────────────────────────────────────────────┐
│        DEVICE A (Desktop)                   │
└─────────────────────────────────────────────┘
User opens app
    │
    ▼
Nuclear clear runs (first time)
    │
    ▼
User logs in
    │
    ▼
Session saved locally on Device A

┌─────────────────────────────────────────────┐
│        DEVICE B (Phone)                     │
└─────────────────────────────────────────────┘
User opens app
    │
    ▼
Nuclear clear runs (first time on this device)
    │
    ▼
User logs in
    │
    ▼
Session saved locally on Device B

┌─────────────────────────────────────────────┐
│        EACH DEVICE INDEPENDENT              │
└─────────────────────────────────────────────┘
- Each device has own localStorage
- Each device runs nuclear clear once
- Each device needs login after update
- Sessions don't sync between devices
  (this is normal for local storage)
```

---

## ⏱️ Timeline View

```
TIME 0: Build 8.0.8 Deployed
│
├─ User 1 opens app (at T+0 min)
│  ├─ Nuclear clear runs
│  ├─ Sees login screen
│  └─ Logs in
│
├─ User 2 opens app (at T+5 min)
│  ├─ Nuclear clear runs
│  ├─ Sees login screen
│  └─ Logs in
│
├─ User 1 reloads page (at T+10 min)
│  ├─ Nuclear clear checks version (matches)
│  ├─ Skips clear
│  └─ Stays logged in
│
├─ User 3 opens app (at T+15 min)
│  ├─ Nuclear clear runs
│  ├─ Sees login screen
│  └─ Logs in
│
└─ All users continue using app normally
   └─ No more nuclear clears
      └─ Sessions persist
         └─ No auth errors
```

---

**Build 8.0.8 Flow Diagrams Complete** ✅
